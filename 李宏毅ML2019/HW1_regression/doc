1. 训练集
    训练集的数据是每个月的前20天，24小时的数据，每18行是一天的18个特征值，将其读取为18*（24*20）的二维数组；
    以月为单位，共20*24=480个小时，再以10小时为一组数据，前9个小时的特征9*18=162个特征为训练数据，后一个小时的PM2.5为label，构成一条数据；
    最终train_X (5652, 162), train_Y (5652,), 扩展trian_X (5652, 163)；

2. 测试集
    测试集是每个月剩余的数据，4320*9，（4320行，9列）每18行是一条9*18=162特征的数据，没有label；
    最终test_X (240, 162)，扩展test_X (240, 163)；

3. 训练
    gradient descent
        权重初始化为0，学习率小一点，（1e-6） 迭代次数上万+；
    Adagradient
        权重初始化为0，学习率大一点， 迭代次数上万+；
4. 预测

